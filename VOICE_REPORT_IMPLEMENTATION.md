# 音声入力報告機能 - 実装詳細

## 📐 アーキテクチャ

```
┌─────────────────────────────────────────────────────────────┐
│                        ユーザー                              │
│                     (週次会議で報告)                         │
└────────────────────┬────────────────────────────────────────┘
                     │ 音声入力
                     ↓
┌─────────────────────────────────────────────────────────────┐
│              VoiceRecorder コンポーネント                     │
│              (components/VoiceRecorder.tsx)                  │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Web Speech API (ブラウザ標準)                         │  │
│  │  - リアルタイム音声認識                                 │  │
│  │  - 日本語対応 (ja-JP)                                   │  │
│  │  - 継続認識 (continuous: true)                          │  │
│  └───────────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────────┘
                     │ テキスト化された音声
                     ↓
┌─────────────────────────────────────────────────────────────┐
│               Gemini API 要約処理                            │
│               (lib/gemini.ts)                                │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  Google Gemini Pro                                     │  │
│  │  - AI要約                                               │  │
│  │  - 構造化（3項目に自動分類）                            │  │
│  │  - JSON形式で出力                                       │  │
│  └───────────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────────┘
                     │ 構造化データ
                     ↓
┌─────────────────────────────────────────────────────────────┐
│              報告ページ (app/reports/page.tsx)              │
│  ┌───────────────────────────────────────────────────────┐  │
│  │  - currentTrial: 今試していること                      │  │
│  │  - progress: 経過報告                                   │  │
│  │  - result: 結果報告・考察                               │  │
│  └───────────────────────────────────────────────────────┘  │
└────────────────────┬────────────────────────────────────────┘
                     │ 保存
                     ↓
┌─────────────────────────────────────────────────────────────┐
│                    Firebase Firestore                        │
│                    (reports コレクション)                     │
└─────────────────────────────────────────────────────────────┘
```

## 🗂️ ファイル構成

### 新規追加ファイル

```
components/VoiceRecorder.tsx      # 音声入力UIコンポーネント
lib/gemini.ts                     # Gemini API統合
VOICE_REPORT_SETUP.md             # セットアップガイド
VOICE_REPORT_IMPLEMENTATION.md    # 実装詳細（このファイル）
```

### 変更ファイル

```
app/reports/page.tsx              # 音声入力機能を統合
.env.local.example                # Gemini APIキー設定追加
README.md                         # 機能説明追加
package.json                      # @google/generative-ai 追加
```

## 🔧 コンポーネント詳細

### VoiceRecorder コンポーネント

**責務**: 音声入力のUI・UXを提供

**主要機能**:
- Web Speech API の初期化と管理
- 録音開始/停止のUI制御
- リアルタイムトランスクリプト表示
- ブラウザ互換性チェック

**Props**:
```typescript
interface VoiceRecorderProps {
  onTranscriptComplete: (transcript: string) => void
  buttonText?: string
  className?: string
}
```

**状態管理**:
- `isRecording`: 録音中フラグ
- `transcript`: 音声認識テキスト
- `isSupported`: ブラウザ対応フラグ

### Gemini API統合 (lib/gemini.ts)

**責務**: 音声テキストのAI要約と構造化

**主要機能**:
```typescript
export async function summarizeReportWithAI(
  transcript: string,
  apiKey: string
): Promise<ReportSummary>
```

**プロンプト設計**:
- 役割設定: 「週次報告会の議事録作成アシスタント」
- 出力形式: JSON（3項目の構造化データ）
- 要約指針: 簡潔、具体的、「です・ます」調

**エラーハンドリング**:
- APIキー検証
- JSON抽出（マークダウンコードブロック対応）
- デフォルト値の設定

## 🔄 データフロー

### 1. 音声入力開始

```typescript
// ユーザーが「🎤 音声で報告を入力」ボタンをクリック
startRecording() {
  recognitionRef.current.start()
  setIsRecording(true)
}
```

### 2. リアルタイム認識

```typescript
// Web Speech API が音声をテキスト化
recognition.onresult = (event) => {
  // 確定したテキストを蓄積
  setTranscript((prev) => prev + finalTranscript)
}
```

### 3. AI要約処理

```typescript
// 「完了してAI要約」ボタンでトリガー
const handleVoiceInput = async (reportId, transcript) => {
  const summary = await summarizeReportWithAI(transcript, apiKey)
  
  // 報告内容を更新
  setReports((prev) =>
    prev.map((r) =>
      r.id === reportId ? { ...r, ...summary } : r
    )
  )
}
```

### 4. 保存

```typescript
// ユーザーが「保存」ボタンをクリック
await saveReports(reports) // Firestore に保存
```

## 🎨 UI/UX設計

### 音声入力ボタン

- **位置**: 各報告カードの上部（編集モード時のみ表示）
- **サイズ**: フル幅、目立つ赤色
- **アイコン**: マイクアイコン🎤

### 録音中UI

- **インジケーター**: アニメーションする赤い点
- **リアルタイム表示**: 認識テキストをスクロール可能エリアに表示
- **アクション**: 「完了してAI要約」「キャンセル」ボタン

### フィードバック

- **処理中**: 「AIで要約中...」トースト通知
- **成功**: 「音声入力を要約して反映しました！」
- **エラー**: 「AI要約に失敗しました。再度お試しください。」

## 🔐 セキュリティ考慮事項

### APIキーの管理

**現在の実装** (フェーズ1):
- `NEXT_PUBLIC_` プレフィックスでクライアント側から利用
- ⚠️ ブラウザに公開される（DevToolsで見える）

**推奨される改善** (フェーズ2):
```
┌──────────┐     ┌──────────────┐     ┌──────────┐
│ Browser  │────▶│ Next.js API  │────▶│ Gemini   │
│          │     │ Route        │     │ API      │
└──────────┘     └──────────────┘     └──────────┘
                  APIキーはサーバー側のみ
```

実装例:
```typescript
// pages/api/summarize.ts
export default async function handler(req, res) {
  const apiKey = process.env.GEMINI_API_KEY // サーバー側のみ
  const summary = await summarizeReportWithAI(req.body.transcript, apiKey)
  res.json(summary)
}
```

### ドメイン制限

Gemini APIキーにドメイン制限を設定:
- 本番: `your-domain.com`
- 開発: `localhost:3000`

## ⚡ パフォーマンス最適化

### 現在の実装

- Web Speech API: ブラウザネイティブ（オーバーヘッドなし）
- Gemini API: ~3-5秒（ネットワーク + AI処理）

### 改善案

1. **ストリーミング応答**:
   ```typescript
   const result = await model.generateContentStream(prompt)
   for await (const chunk of result.stream) {
     // リアルタイムで部分結果を表示
   }
   ```

2. **キャッシング**:
   - プロンプトテンプレートのキャッシング
   - よく使う表現の事前処理

3. **並列処理**:
   - 複数メンバーの報告を並列でAI処理

## 🧪 テスト戦略

### 単体テスト

```typescript
// lib/gemini.test.ts
describe('summarizeReportWithAI', () => {
  it('should return structured summary', async () => {
    const transcript = '今週は...'
    const summary = await summarizeReportWithAI(transcript, apiKey)
    expect(summary).toHaveProperty('currentTrial')
    expect(summary).toHaveProperty('progress')
    expect(summary).toHaveProperty('result')
  })
})
```

### 統合テスト

```bash
npx ts-node tmp_rovodev_test_voice_integration.ts
```

### E2Eテスト（手動）

1. 開発サーバー起動
2. ログイン → 編集モード
3. 音声入力ボタンをクリック
4. 報告を話す
5. AI要約を確認
6. 保存して再読み込み

## 📊 コスト見積もり

### Gemini API無料枠

- **60 リクエスト/分**
- **1,500 リクエスト/日**

### 週次会議の例

- メンバー数: 10人
- 報告時間: 1人5分
- 修正・再実行: 各1回まで

**月間コスト**: 0円（無料枠内）

## 🚀 今後の拡張

### フェーズ2: 高度な音声認識

- Google Cloud Speech-to-Text統合
- より高精度な認識
- 複数言語対応

### フェーズ3: カスタマイズ

- プロンプトテンプレート編集UI
- 報告フォーマットのカスタマイズ
- メンバー別の要約スタイル

### フェーズ4: 分析機能

- 過去の報告との比較
- キーワード抽出
- トレンド分析

## 🐛 既知の制限事項

1. **ブラウザ依存**: Firefox は Web Speech API 非対応
2. **ネットワーク必須**: オフラインでは動作しない
3. **ノイズ感度**: 静かな環境が望ましい
4. **言語固定**: 現在は日本語のみ

## 📚 参考資料

- [Web Speech API - MDN](https://developer.mozilla.org/en-US/docs/Web/API/Web_Speech_API)
- [Google Gemini API Documentation](https://ai.google.dev/docs)
- [Next.js Environment Variables](https://nextjs.org/docs/basic-features/environment-variables)
